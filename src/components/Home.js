import styles from '@/styles/Home.module.css'
/**
 * Renders Scoop home page
 * @returns {JSX.Element}
 * @constructor
 */
export default function Home() {
    return (
        <div className={styles.home}>
            <h1>SCOOP</h1>
        </div>
    );
}